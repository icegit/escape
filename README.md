# Escape

A static travel dashboard with destination cards, countdowns, trip details and itinerary information from `travels.json`.

Personal health and Garmin features have moved to the private `icegit/wellness` repository and its access-controlled site.

## Local verification

```powershell
npm ci --ignore-scripts
npm test
```
